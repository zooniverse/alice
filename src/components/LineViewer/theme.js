const theme = {
  textInput: {
    container: {
      extend: props => `
        input {
          background: #DEDEDE;
          font-style: italic;
          padding: 0.25em;
        }
      `
    }
  }
}

export default theme
