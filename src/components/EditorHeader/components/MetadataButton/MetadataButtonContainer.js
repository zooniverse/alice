import React from 'react'
import MetadataButton from './MetadataButton'
import metadata from './mockMetadata'

export default function MetadataButtonContainer() {
  return <MetadataButton metadata={metadata} />
}
